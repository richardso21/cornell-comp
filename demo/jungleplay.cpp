#include <iostream>
using namespace std;

int main(void)
{
    int N, K, M;
    cin >> N >> K >> M;
    string seq;
    cin >> seq;

    int knocked = 0;
    int times = K + 1;
    while (times > 0)
    {
        times--;
        
    }
}